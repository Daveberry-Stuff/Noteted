# got this off google stfu lmao
# anyways, github workflow for Windows, MacOS and Linux.

name: Python package

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

env:
    pythonVersion: 3.13

jobs:
    Linux:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Set up Python ${{ env.pythonVersion }}
              uses: actions/setup-python@v2
              with:
                python-version: ${{ env.pythonVersion }}
            - name: Install dependencies
              run: |
                python -m pip install --upgrade pip
                pip install -r requirements.txt
                sudo apt-get install python3-tk 
            - name: Compile Linux
              run: |
                pyinstaller Noteted.spec
            - name: Upload Built for Linux
              uses: actions/upload-artifact@v4
              with:
                name: 'Noteted'
                path: 'dist'
    Windows:
        runs-on: windows-latest
        steps:
            - uses: actions/checkout@v2
            - name: Set up Python ${{ env.pythonVersion }}
              uses: actions/setup-python@v2
              with:
                python-version: ${{ env.pythonVersion }}
            - name: Install dependencies
              run: |
                python -m pip install --upgrade pip
                pip install -r requirements.txt
            - name: Compile Windows
              run: |
                pyinstaller Noteted.spec
            - name: Upload Built for Windows
              uses: actions/upload-artifact@v4
              with:
                name: 'Noteted'
                path: 'dist'
    MacOS:
        runs-on: macos-latest
        steps:
            - uses: actions/checkout@v2
            - name: Set up Python ${{ env.pythonVersion }}
              uses: actions/setup-python@v2
              with:
                python-version: ${{ env.pythonVersion }}
            - name: Install dependencies
              run: |
                python -m pip install --upgrade pip
                pip install -r requirements.txt
            - name: Compile MacOS
              run: |
                pyinstaller Noteted.spec
            - name: Upload Built for MacOS
              uses: actions/upload-artifact@v4
              with:
                name: 'Noteted'
                path: 'dist'
                